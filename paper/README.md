This directory contains the tex source and the compiled PDF of the paper for this course project.
The paper is named `main.pdf`.
You can recompile the paper by running `make` in this directory.
Run `make clean` to remove generated files.
